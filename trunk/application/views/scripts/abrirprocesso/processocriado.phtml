<?php 
$this->breadcrumbs()->addPage('Abrir Processo', 'abrirprocesso');
$this->breadcrumbs()->addPage('Resumo do Processo', 'abrirprocesso', 'processocriado');
echo $this->breadcrumbs()->render(); 

echo $this->pagenav()->openList(); 
echo $this->pagenav()->defaultHelperLinks();
echo $this->pagenav()->closeList();
?>

<div id="article">
    <fieldset>
        <legend>Dados Gerais</legend>
        <?php
            echo $this->fieldList()->open();
            echo $this->fieldList()->text('Tipo de Processo', $this->processoCriado->tipoProcesso[0]->nome);
            echo $this->fieldList()->text('Assunto', $this->processoCriado->assunto[0]->nome);
            echo $this->fieldList()->text('Data', $this->processoCriado->data);
            echo $this->fieldList()->text('Número', str_replace("_", "/", $this->processoCriado->nome));
            echo $this->fieldList()->text('Nº Origem', $this->processoCriado->numeroOrigem);
            echo $this->fieldList()->close();
        ?>
    </fieldset>
    <fieldset>
        <legend>Manifestante</legend>
        <?php
            echo $this->fieldList()->text('CPF/CNPJ', $this->processoCriado->manifestante[0]->cpfCnpj);
            echo $this->fieldList()->text('Nome', $this->processoCriado->manifestante[0]->nome);
            echo $this->fieldList()->text('Bairro', $this->processoCriado->manifestante[0]->bairro[0]->nome);
            echo $this->fieldList()->text('Tipo', $this->processoCriado->tipoManifestante[0]->nome);
            echo $this->fieldList()->text('Responsável', '');
        ?>
    </fieldset>
    <fieldset>
        <legend>Processo</legend>
        <?php
            echo $this->fieldList()->text('Corpo', $this->processoCriado->corpo);
            echo $this->fieldList()->text('Prioridade', $this->processoCriado->prioridade[0]->nome);
            echo $this->fieldList()->text('Local Atual', $this->processoCriado->localAtual[0]->nome);
            echo $this->fieldList()->text('Data Prazo', $this->processoCriado->dataPrazo);
        ?>
    </fieldset>
</div>
