<?php 
header('Content-Type: application/json');
$output = '[';
for ($i = 0; $i < count($this->assuntos); $i++) {
    if ($i > 0) {
        $output .= ',';
    }
    $output .= '{"id":"'. $this->assuntos[$i]->id . '", "name":"' . $this->assuntos[$i]->nome . '"}';
}
$output .= ']';

echo $output;
?>